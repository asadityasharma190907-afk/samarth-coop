title: "feat(epic-4): [Story 4.4] Wallet & Completion UI"
body: |
  Closes #91

  ## Summary
  Built the citizen-facing post-completion UI (Welfare Fund animated chip) and the worker-facing Wallet view to make the cooperative platform's financial models highly visible.

  ## Approach
  - **Wallet Page**: 
    - `WorkerWallet.tsx` utilizing `useWallet` to fetch `/wallet/{worker_id}`.
    - Weekly and Lifetime earnings displayed prominently in monospace font as specified by `DESIGN.md`.
    - Integrated `EarningsTable.tsx` for history log.
  - **Completion UI**: 
    - Created `WelfareFundChip.tsx` using `var(--color-welfare-fund)` violet gradient.
    - Added an ease-out quartic 500ms JS count-up animation on mount for the welfare amount.
  - **Routing**: Added `/worker/wallet` and hooked it into the existing `BottomTabNav`.
  - **Auth Context**: Updated `useAuth()` to safely decode the JWT `samarth_token` to retrieve the `sub` (worker ID) without needing external dependencies.

  ## Testing
  - Types verified successfully with `tsc --noEmit`.
